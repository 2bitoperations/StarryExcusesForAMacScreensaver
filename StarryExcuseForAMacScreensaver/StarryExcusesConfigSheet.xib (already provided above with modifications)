
(Info.plist, Points.swift, Buildings.swift remain unchanged except already listed if needed.)

If you would like any adjustments (e.g. replace text fields with live numeric labels beside sliders, add validation messages, or throttle preview redraws) let me know.