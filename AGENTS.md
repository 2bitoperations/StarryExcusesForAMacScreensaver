# AI Agents Instructions

I always, always, always want you to discuss any changes you want to make (and their tradeoffs, if any,) before actually making them.

This project is supposed to serve as a fun learning test bed for agentic programming, and as such, our tone should always be lighthearted and welcoming.

## macOS Screensaver Build & Test Workflow

After running `build-saver.sh` (which installs the new `.saver` into `~/Library/Screen Savers/`), the `legacyScreenSaver` host process **must be restarted** before testing — macOS does **not** automatically reload the bundle when it is replaced on disk.

```bash
killall legacyScreenSaver
```

Then reopen System Settings → Screen Saver. The fresh process picks up the new binary. Skipping this step means you are testing stale code and will draw wrong conclusions about what is or isn't working.

## File Contents

### Core
 - `StarryExcuseForAView.swift` - Main entrypoint for the screensaver, where the hooks from the OS live, also where configuration options are read into the engine.
 - `StarryEngine.swift` - Orchestrates the overall simulation loop, configuration changes, timing, renderer creation, and per-frame data assembly (sprites, moon params, clears) for GPU or headless rendering. Also emits automatic planet-moon point sprites (Galilean moons + Titan) into the shared sprite path.
 - `Skyline.swift` - Generates and maintains the static structural world state (buildings, flasher geometry, and moon) plus utility methods to sample stars and building light points while managing timed clearing and moon/flasher behavior.

### Simulation Layer Renderers
 - `SkylineCoreRenderer.swift` - Converts the evolving skyline simulation into per-frame sprite instances by time-based spawning of stars, building lights, and an optional flasher using configured per-second rates.
 - `ShootingStarsLayerRenderer.swift` - Simulates shooting stars with randomized trajectories, brightness curves, and configurable spawn rates. Produces additive sprites that leave trails.
 - `SatellitesLayerRenderer.swift` - Simulates satellite point-lights traversing the sky on randomized great-circle-ish paths with configurable speed, brightness, and spawn rates.
 - `MoonLayerRenderer.swift` - Computes the moon's screen position along a traversal arc and its phase angle, producing `MoonParams` for the GPU moon shader.
 - `DebugLayerRenderer.swift` - Generates the FPS counter and CPU usage debug overlay sprites when debug mode is enabled.

### Data Types & Helpers
 - `MetalTypes.swift` - Defines GPU-shared data types: `SpriteInstance`, `SpriteShape`, `MoonUniforms`, `PlanetParams`, `StarryDrawData`, and related enums/structs.
 - `Moon.swift` - Moon phase calculation (based on real-world lunar cycle) and traversal path geometry.
 - `MoonTexture.swift` - Procedural generation of the moon's albedo texture (cratered, noisy lunar surface).
 - `Planet.swift` - Keplerian orbital ephemeris for all 8 planets (Mercury, Venus, Mars, Jupiter, Saturn, Uranus, Neptune, Pluto), altitude/azimuth sky position from a hardcoded observer location, and screen-space mapping with three positioning modes: "random" (always randomised), "hide" (hidden when below horizon), or "randomWhenBelow" (orbital when visible, random otherwise). Positions use a per-engine nonce so "Regenerate Preview" shuffles them. Includes Saturn ring tilt calculation (Schlyter formula) and lightweight circular-orbit helpers for Jupiter/Saturn moon dot positions.
 - `PlanetTexture.swift` - Procedural RGBA texture generation for all 8 planets in a retro pixel-art style: Jupiter (horizontal bands, Great Red Spot, limb darkening, per-pixel noise), Saturn (square body-only texture — rings rendered geometrically in shader), Venus/Mars/Mercury (solid surfaces with atmospheric or weathered effects), and outer planets (gas giants and ice giants with varying detail and color schemes).
 - `Buildings.swift` - Building style definitions and tile patterns used to generate the skyline silhouette.
 - `Points.swift` - Lightweight `Point` and `Color` value types used throughout the simulation.
 - `DebugSprites.swift` - Helper to generate debug outline rectangle sprites for visual debugging.

### GPU Rendering
 - `StarryMetalRenderer.swift` - The Metal rendering pipeline: manages GPU resources, pipelines, offscreen textures, sprite upload, per-frame scene encoding, compositing, and moon rendering. See [METAL_RENDERER_MAP.md](METAL_RENDERER_MAP.md) for an architectural overview.

### Configuration
 - `StarryConfigSheetController.swift` - Handles drawing the screensaver options panel and associated controls, including per-planet size sliders, planetary position mode dropdown, Saturn ring tilt override (Automatic/Manual with angle slider), and planet terminator mode.
 - `StarryDefaultsManager.swift` - Handles storing and fetching screensaver options values. Reasonable default fallback values for each option live here. Accepts an optional `moduleIdentifier` parameter to override the defaults domain.
 - `BuildInfo.swift` - Auto-generated at build time by a Run Script phase. Contains the git commit hash constant `buildCommit`.

### Preview App (`PreviewApp/`)
 - `main.swift` - Entry point for the standalone StarryPreview app.
 - `PreviewAppDelegate.swift` - Creates a borderless window hosting the screensaver view. Reads the user's saved settings from the sandboxed screensaver container (see Known Quirks below) and displays a build-info overlay.
 - `PreviewApp-Info.plist` - Minimal app plist for the preview target.


## Rust + wgpu port (`starry-rs/`)

A cross-platform Rust + wgpu rewrite lives in [`starry-rs/`](starry-rs/). The plan is to eventually retire the Swift codebase on every platform — including macOS — once the Rust side reaches feature parity. Until then the Swift code remains the visual ground-truth and the shipping macOS product, and both trees live happily side by side in this repo.

### Status (Phase 6b — June 2026)
Windowed app and headless `--dump-png` mode both drive a real `Engine` that ports `StarryEngine.swift` + `Skyline.swift` + `SkylineCoreRenderer.swift` + `Buildings.swift` + `ShootingStarsLayerRenderer.swift` + `SatellitesLayerRenderer.swift` + `Moon.swift` + `MoonTexture.swift` + `Planet.swift` + `PlanetTexture.swift`: seeded skyline generation, horizon-weighted stars, building windows with random per-tile light states, rate-clocked sprite emission, periodic clear, **a flashing beacon on the tallest building with `Over`-blended snap-on + exponential decay half-life (default 0.08s, snappy-LED feel)**, **additive shooting-star streaks with decaying trails (`keep = 0.5^(dt/halfLife)`)**, **additive satellite point-lights traversing a flasher-constrained band with their own decay half-life**, a **procedural moon with real-world Julian-day phase math, traversal-arc motion, 3 terminator modes (hard / smooth / banded), and a 64×64-base-albedo R8Unorm texture upsampled per-canvas-width**, and **all 8 procedural planets (Mercury / Venus / Mars / Jupiter / Saturn / Uranus / Neptune / Pluto) with Keplerian J2000 ephemeris + alt/az→screen mapping (hardcoded Austin TX observer), 3 positioning modes (random / hide / random-when-below), per-planet RGBA8 procedural textures with mipmap chains generated at upload, nearest-filter sampler for retro pixel-art parity, and a single shared `planet.wgsl` pipeline parameterized by per-planet UBO — Saturn renders with a banded pale-gold body texture inside a stretched-aspect quad (texture_aspect = 2.0) plus geometric 7-zone rings (C / B-inner / B-outer / Cassini gap / A-inner / Encke gap / A-outer) computed in the fragment via Schlyter B-formula tilt + celestial-pole position-angle rotation, in 3 user-pickable styles (smooth / flat-retro / chunky-pixel) with `Option<f64>` manual angle overrides**, plus **automatic point-sprite emission for the Galilean moons (Io / Europa / Ganymede / Callisto) and Saturn's Titan via per-moon Keplerian orbital ephemeris (J2000 epoch, foreshorten Y by parent ring/spin tilt, 2D rotation by ring-rotation angle, scale by parent radius, near/far-side z-cull) — drawn over the composite as `BlendMode::Over` premultiplied alpha at α = 0.78, switchable to debug-colors mode (Io=red, Europa=green, Ganymede=blue, Callisto=cyan, Titan=yellow at α = 1.0) via the same `--debug-moon-colors` flag that toggles raw-albedo lunar-moon mode**. Rendering uses four persistent FBOs (skyline + three ping-pong pairs for flasher/satellites/shooting) orchestrated through an 8-pass per-frame encode (sat decay → shoot decay → flasher decay → skyline sprites → sat sprites → shoot sprites → flasher sprite → composite), with stateless N-layer composite blending in Swift Z-order (skyline → flasher → satellites → shooting), and the **planets + planet-moons + moon drawn inside the composite pass *after* the N-layer composite (premultiplied alpha; planets first, then planet-moons on top of them, then the lunar moon on top of everything)**. Disabling any layer via `--*-enabled false` (or `--flasher-period-s 0`, `--planets-enabled false`, `--planet-moons-enabled false`) skips GPU resource allocation entirely. CLI surfaces all 61 knobs (12 Phase-2 + 15 Phase-3 + 11 Phase-4 + 12 Phase-5a + 3 Phase-5b + 3 Phase-5c + 1 Phase-6a + 3 Phase-6c + 1 Phase-6b) via clap with defaults mirroring `StarryDefaultsManager.swift`. With Phase 6b done the Rust port is at **full feature parity with the shipping Swift macOS build including the debug HUD**. All Phase 6 sub-phases (6a TOML config, 6c deterministic-seed mode, 6b debug overlay) complete; v0.1 milestone reached.

**Phase 3.5** split the original single-crate `starry-rs` into a **Cargo workspace** with two members: `starry-core` (library — all simulation + GPU pipelines + WGSL shaders + headless renderer, window-agnostic) and `starry-app` (binary — the thin winit-driven shell that owns the Surface + main event loop + CLI dispatch). Headless SHA `476e74bca9286c4ec046a288e2f567eb4a5109f39544a3c4cf20877439e6c044` is byte-identical pre- and post-split, proving zero-impact refactor.

**Phase 4** added the procedural moon: ported `Moon.swift` (Julian-day phase math + traversal arc + override triangular wave), `MoonTexture.swift` (64×64 base albedo with 7 hardcoded maria + per-pixel hash noise + CPU nearest upsample to per-canvas-width diameter), and `Shaders.metal` lines 152-266 (vertex + fragment with 3 terminator modes + soft-edge feather + debug-colors override). Wall-clock time is now an **injected parameter** (`Engine::frame(wall_now: SystemTime)`) — windowed passes `SystemTime::now()`, headless anchors at hardcoded `HEADLESS_NOW_UNIX_SECS = 1_704_067_200` (2024-01-01 UTC) for byte-stable determinism. Default terminator mode is **`1` (smooth)** in both Rust and Swift — hard mode produced a strong Mach-band perceptual illusion when the moon was large in frame (sharp brightness contrast → eye perceives a dark stripe at the terminator that isn't in the pixels). SHA baseline `986155e2c009a4dbf63227d32c4171d80a43cbb05a5a54852148ac52a752fa34` — different from Phase 3.5 (`476e74bc...`) because the moon is now in frame, as expected.

**Phase 5a** added 7 of the 8 procedural planets (Mercury / Venus / Mars / Jupiter / Uranus / Neptune / Pluto — Saturn body + rings deferred to 5b): ported `Planet.swift` (Keplerian J2000 ephemeris + alt/az→screen mapping with a hardcoded Austin TX observer + per-engine `nonce: u64` rooted in `Config::seed` for deterministic random fallback positions + 3 below-horizon modes [random / hide / random-when-below]) and `PlanetTexture.swift` (7 procedural RGBA8 generators in retro pixel-art style, with a mipmap chain generated at upload time for Swift parity, sampled with `nearest` to preserve the pixel-art look — intentionally different from the moon's `linear` filter). All 7 planets share a single `planet.wgsl` pipeline parameterized by a 13-field per-planet UBO packed into 3 `vec4`s; the cache is keyed by `PlanetIdentity` (8 variants, no Earth) for O(1) per-planet texture lookup and selective regeneration on resize. The composite pass now draws planets *before* the moon (and Phase 5c will slot planet-moons between them) — verified Swift parity against `StarryMetalRenderer.swift:1648/1668/1692`. A follow-up commit landed a 1-line subtractive eps in `decay.wgsl` (`max(s * keep − 1/2048, 0)`) to escape the sRGB-8 quantization fixed point that left a permanent shooting-star trail residue in the windowed app — the math is `N* ≤ floor(0.5 / (1 − keep))` so shoot keep=0.938 stuck at sRGB-8 (visible); eps ≈ 0.000488 linear shifts the rounding outcome at the low end without perceptibly changing brighter values. Headless SHAs: planets-default `bc6fd368…3e2b`; planets-off `986155e2…fa34` (matches Phase-4 baseline byte-for-byte, proving `--planets-enabled false` fully Option-skips planet resources); eps fix is byte-identical for both (single-frame headless skips decay entirely).

**Phase 5b** filled in the 8th planet (Saturn) along with its rings — the Phase-5a `PlanetIdentity::Saturn` slot was always going to land here and now it does. Ported `Planet.swift` Saturn-specific math: `saturnRingTilt(now)` (Schlyter B-formula: `B = arcsin(sin β · cos 28.06° − cos β · sin 28.06° · sin(λ − Nr))`) + `saturnRingPositionAngle(now)` (rotation derived from Saturn's RA/Dec relative to the celestial pole `α=40.589°, δ=83.537°`), both wrapped in a single `saturn_ring_state(now)` standalone function in `planet.rs`. Ported `PlanetTexture.swift::createSaturnTexture` (`generate_saturn_albedo_map` + `saturn_body_color`): a square pale-gold banded body-only texture with double limb darkening — no rings baked in, since the rings are rendered geometrically. Extended `planet.wgsl` with a Saturn fragment branch (discriminator `is_saturn = aspect > 1.0`, matching `Shaders.metal:312`): vertex stretches the quad to `aspect=2.0` so the rings have horizontal real-estate; fragment renders the body inside `r ≤ 0.846` and rings across `r ∈ [0.93, 1.95]` with a 7-zone radial band selector (C / B-inner / B-outer / Cassini gap / A-inner / Encke gap / A-outer), an edge-on guard `|sin_tilt| ≥ 0.01`, and 3 user-pickable styles (`smooth` continuous / `flat-retro` 4-color luminance quant + 2×2 Bayer alpha dither / `chunky-pixel` 16-step radial quant + checker dither). Three new CLI flags (`--saturn-ring-style {smooth|flat-retro|chunky-pixel}`, `--saturn-ring-tilt-angle <-27..27>`, `--saturn-ring-rotation-angle <0..360>`) — the two angle flags are `Option<f64>` so absent = automatic, present = manual override; this collapses Swift's two-flag-pair (mode + manual-angle) UX into one knob per axis. Headless SHAs: planets-default `fc7d5fdcb431da1566a004d5b19cf3f2c4193311043eb89984f856e60f76ebb6` (Saturn appears, was 7 planets in Phase 5a, now 8); planets-off `986155e2…fa34` byte-identical to Phase 4 baseline (planet-resource gating still works through the new Saturn shader path). 32/32 unit tests pass (was 29 pre-5b: +2 ring-state tests in `planet.rs`, +2 banded-texture tests in `planet_texture.rs`, −1 dropped flat-gold placeholder).

**Phase 5c** brought the Rust port to **feature parity with the shipping Swift macOS build** by adding automatic point-sprite emission for the four Galilean moons (Io, Europa, Ganymede, Callisto) and Saturn's Titan. Ported `Planet.swift::moonSpriteStates` to a `moon_sprite_states(parent, wall_now, parent_radius_px, parent_center_px, ring_tilt_deg, ring_rotation_deg, scale)` standalone function in `planet.rs` (+~207L production + 5 unit tests): per-moon orbital constants (semi-major axis in parent-radii, sidereal period in days, initial phase) → 2π·days/period angle → ecliptic XY → foreshorten Y by `sin(ring_tilt_deg)` (3° hardcoded for Jupiter; Saturn uses live ring-tilt) → 2D rotate by ring-rotation → scale by parent radius → near/far-side z-cull (skip moons with `y > 0 && dist < parent_radius`). Sprite size = `clamp(base_size · moon_scale · clamp(parent_r / base_ref, 0.6, 1.8), 1.0, 3.0)` with `base_ref = 24` (Jupiter) / `20.3` (Saturn) — preserves Swift's perceptual-size scaling. Ported `StarryEngine.swift::debugMoonColorPremul` to `types.rs::debug_moon_color_premul(name)` (zero-alloc lowercase match returning premultiplied-alpha RGBA per moon). The `engine.rs::FrameOutput` grew an 8th field (`planet_moons: &'a [SpriteInstance]`) populated by an inline single-pass emission loop in `frame_impl` (Rust has access to the moon name at sprite construction time, so the `--debug-moon-colors` color override happens in the same loop — cleaner than Swift's two-pass approach). The `gpu.rs::GpuPipelines` gained an `Option<SpriteRenderer>` field for planet-moons (capacity 5, `BlendMode::Over`, gated on `app_config.planet_moons_enabled` so disabling fully Option-skips pipeline + VBO + viewport-UBO allocation); the renderer drains its instance buffer pre-encoder and the draw call slots into the existing composite pass between `planets.draw_all()` and `moon.draw()` (3 sub-draws inside the same composite pass — encode-pass count stays at 8). The `headless.rs` plan grew from 6 passes to 7 (planet-moons inserted between Pass 5 planets and the now-renamed Pass 7 moon, gated lazily on `!frame_output.planet_moons.is_empty()`). Three new CLI flags (`--planet-moons-enabled <bool>`, `--jupiter-moon-scale <f64>`, `--saturn-moon-scale <f64>`); the existing `--debug-moon-colors` flag does double-duty toggling both lunar-moon raw-albedo and per-planet-moon hue-distinction modes (Swift parity). Headless SHAs (seed=42 1280×800 dt=5.0s, anchor 2024-01-01 UTC): planets-default `1e0f0067b1e16febe9668f7d2329886e35bbd95d8b64c15bce01b8355d79be5d` (frame log shows `planet_moons=5` — full max-capacity exercise: 4 Galileans + Titan); `--debug-moon-colors true` → `b2f42ad31f8efa5e7ac46c1d44a24c2fea11a32f87b6246cf771d1c2a4253cc8` (3rd distinct SHA, proves the override path); `--planet-moons-enabled false` → `fc7d5fdcb431da1566a004d5b19cf3f2c4193311043eb89984f856e60f76ebb6` byte-identical to the Phase 5b baseline (proves `Option<SpriteRenderer>` cleanly skips planet-moon GPU resources without altering anything else). 39/39 unit tests pass (was 32 pre-5c: +5 moon-state tests in `planet.rs`, +2 debug-color tests in `types.rs`).

**Phase 6a** added a TOML config loader (~700 LOC) layered over clap. Three-tier precedence: clap defaults < TOML file < explicit CLI flags. New `--config <path>` flag triggers a hard error if the named file is missing (an explicit flag is an explicit promise); otherwise the loader auto-discovers `$XDG_CONFIG_HOME/starry/config.toml` then `./starry.toml`, silently falling through if absent. Hand-rolled `PartialConfig` (in new `toml_config.rs`) mirrors `Config`'s 58 non-`--config` fields wrapped in `Option<T>`, derives `Deserialize` with `#[serde(deny_unknown_fields, rename_all = "kebab-case")]` (typos like `start-fraction` instead of `stars-fraction` trigger parse errors at load time), and exposes `apply_over(&self, dst: &mut Config)` + `cli_partial_from_matches(&ArgMatches)` (the latter uses `ArgMatches::value_source(name) == ValueSource::CommandLine` to distinguish "user typed `--seed 42`" from "clap filled in default 42"). The 3 existing `Option<T>` Config fields (`dump_png`, `saturn_ring_tilt_angle`, `saturn_ring_rotation_angle`) stay `Option<T>` in `PartialConfig` — TOML key absent = no override, present = override; you can't TOML-set them back to `None` (CLI-only operation). Path A (hand-rolled) was picked over the `config` crate for transparency (~20 obvious lines of precedence wiring instead of a black-box dep chain) and minimal-deps philosophy (serde + toml only, no extra 12 transitive deps). Trade-off: every new flag = 4 edits (Config + PartialConfig + cli_partial extractor + apply_over setter), guarded by the `partial_field_count_matches_config` test which hard-codes `EXPECTED = 58`. The `--config` flag itself is intentionally NOT in `PartialConfig` (would create chicken/egg if a TOML could specify its own path). `lib.rs` now declares 20 pub mods (was 19). 46/46 unit tests pass (was 39 pre-6a: +7 toml_config tests). 3-SHA Phase 6a proof matrix: no-TOML default → `1e0f0067…` **byte-identical to Phase 5c baseline** (zero behavior change without a TOML file present); TOML `seed=999` alone → `03c1e9cc…` (proves TOML applies; skyline sprite count 591→582); TOML `seed=999` + CLI `--seed 42` → `1e0f0067…` back to baseline (proves CLI override wins).

**Phase 6c** added deterministic-seed mode via 3 new CLI flags: `--time-mode {realtime|deterministic|frozen}` (default `realtime`) + `--fixed-dt <f64>` (default `1.0/60.0`) + `--time-anchor <u64>` (default `1_704_067_200` = 2024-01-01 UTC, byte-identical to headless). The new `TimeMode` enum (in `config.rs`, serde-derived for TOML) drives per-frame clock dispatch in `starry-app/src/app.rs`: `realtime` = `SystemTime::now()` + frame-paced dt clamped to `MAX_DT_SECONDS = 0.25`; `deterministic` = `anchor + frame_count × fixed_dt` + dt = fixed_dt (clamp skipped — caller is trusted); `frozen` = `anchor` + dt = 0 (decay layers neither accumulate nor decay; cinematic still mode). `frame_count: u64` lives on the App struct (NOT the Engine — the simulator stays pure w.r.t. frame indexing); resets on `Resized` so the moon doesn't teleport after a window resize. Defensive `.max(0.0)` on the deterministic time math guards against malformed negative `--fixed-dt` panicking `Duration::from_secs_f64`. `HEADLESS_NOW_UNIX_SECS` was promoted from a `headless.rs` local const to a `pub const` in `config.rs` so windowed det-mode default and headless mode share the same literal — `headless.rs` now imports it like everyone else. Headless `--dump-png` ignores `--time-mode` entirely (it already has its own fixed dt + anchor schedule, layering det-mode on top would just confuse two orthogonal mechanisms). **No new unit tests added** (originally-scoped 49-test target was abandoned — det/frozen behavior lives in the windowed `app.rs` path which doesn't have a cheap unit-test seam; SHA-parity against the realtime-default headless covers regression). **Golden-image diff via `cargo test` deferred** to a follow-up when it becomes a felt need. Field counts: Config 59 → 62 pub fields; PartialConfig 58 → 61; field-count guard pinned `EXPECTED = 58 → 61` with assertion msg `"phase-6c anchor"`. Headless SHA: default `--dump-png` @ seed=42 1280×800 dt=5.0s → `1e0f0067…` **byte-identical to Phase 6a + Phase 5c baseline** — proves `realtime` is the default and the 3 new flags don't perturb existing paths. **46/46 unit tests pass** (same count as Phase 6a, per decision above).

**Phase 6d** kicked off a small "measure-then-fix" CPU-efficiency campaign with a benchmark harness as the very first step. Six parallel background-agent investigations surfaced an A-G ranked fix list — top of the heap: winit `ControlFlow::Poll` busy-spinning the main loop (Fix A), zero `[profile.dev]` overrides on the debug build (Fix B), and Saturn/Jupiter trig-storms recomputed every frame (Fix D). To make those gains *measurable* before touching anything, the harness lives in `starry-core/src/bench.rs` (~340 LOC, gated on new `--bench-frames N` + `--bench-tag <label>` flags) and reuses the real `GpuPipelines::render_to_view` path — same render orchestration as the windowed shell, just rendering into a single offscreen `Rgba8UnormSrgb` texture with no Surface attached. Each invocation runs 1 warmup frame (discarded — absorbs lazy GPU init / shader compile / page-in skew that otherwise wrecks p99+max), then N timed frames each ending in `device.poll(wgpu::PollType::wait_indefinitely())?` so per-frame deltas reflect GPU completion (not just submission), with `CpuSampler` taken pre+post loop for an honest cpu/frame number. Stats are nearest-rank percentile (matches `cargo bench` and hyperfine; simpler than linear interp); output is a stdout summary AND one appended row to `starry-rs/bench-results.md` (auto-headered on first write, schema `| timestamp | tag | git_hash | frames | size | seed | mean µs | p50 µs | p95 µs | p99 µs | max µs | wall s | cpu s | cpu/frame µs |`). Default tag is `git rev-parse --short HEAD` + `+dirty` suffix from `git status --porcelain`, falling back to `"unknown"` if git isn't around. Baselines captured at HEAD `0472ae2+dirty` on Apple M1 Max / Metal / 1280×800 / seed=42 / 600 frames: **release mean = 1517 µs/frame, p99 = 1808 µs, cpu/frame = 519 µs**; **debug mean = 3097 µs/frame, p99 = 3641 µs, cpu/frame = 2093 µs**. Both are comfortably under the 16.67 ms vsync budget — confirming the real felt CPU pain in windowed mode is the Poll busy-spin, not per-frame render work. **Big caveat** (called out in the bench.rs module docstring): the harness has no Surface, so Fix A (Poll→Wait + `pre_present_notify`) and Fix C (explicit Fifo `present_mode`) cannot be observed here — those gains have to be measured via the dogfooded Phase 6b debug overlay running against the real winit shell. Config gained 2 fields (now 65 user-facing); PartialConfig mirrors 64; field-count guard bumped `EXPECTED = 64` with anchor comment `"phase-6d"`; lib.rs grew to 24 `pub mod` (bench joins alphabetically before `buildings`); 60/60 unit tests pass; clippy clean. Headless `--dump-png` SHA `1e0f0067…` byte-identical to Phase 6b baseline (bench harness lives in a sibling module, never touches the headless path).

See the phased roadmap in [`starry-rs/README.md`](starry-rs/README.md) and the granular engineering scratchpad in [`starry-rs/PORT_PLAN.md`](starry-rs/PORT_PLAN.md).

### File layout (will grow as phases land)

**Workspace root:**
 - `starry-rs/Cargo.toml` - Workspace root (`resolver = "3"`, `members = ["starry-core", "starry-app"]`, `default-members = ["starry-app"]` so `cargo run` from workspace root targets the binary). `[workspace.package]` shares version/edition/license metadata; `[workspace.dependencies]` pins all crate versions in one place. Deps pinned: wgpu 29, winit 0.30, pollster 0.4, log 0.4, env_logger 0.11, rand 0.8, bytemuck 1 (derive), png 0.17, clap 4 (derive). Release profile uses `lto = "thin"` and `codegen-units = 1`.

**`starry-core/` — library crate (window-agnostic; all simulation + GPU pipelines + WGSL + headless):**
 - `starry-rs/starry-core/Cargo.toml` - Library manifest. Deps via `.workspace = true`: wgpu, log, rand, bytemuck, png, clap, pollster (pollster is needed by `headless.rs` for blocking device acquisition).
 - `starry-rs/starry-core/src/lib.rs` - Crate root: 24 `pub mod` declarations (`bench`, `config`, `toml_config`, `types`, `buildings`, `skyline`, `skyline_renderer`, `shooting_stars`, `satellites`, `moon`, `moon_texture`, `moon_renderer`, `planet`, `planet_texture`, `planet_renderer`, `engine`, `gpu`, `sprite`, `decay`, `composite`, `font`, `cpu_sample`, `debug_overlay`, `headless`). No re-exports — call sites use explicit paths like `starry_core::config::Config`.
 - `starry-rs/starry-core/src/buildings.rs` - 6 `BuildingStyle` 8×8 tile patterns + `Building` (start_x, start_y, width, height, style, lights_on bitmap) + `contains` + `is_light_on` (samples the style pattern wrapped over the building rectangle).
 - `starry-rs/starry-core/src/skyline.rs` - `Skyline` static world: buildings (Z-ordered topmost-by-`start_x`), per-column `sky_floor` for star rejection sampling, optional flasher on the tallest building, periodic-clear timer, **`building_max_height: i32`** for downstream layers (e.g. ShootingStars `safeMinY`). Key methods: `attempt_star` (single-shot rejection — caller drives the attempt count), `sample_building_light` (bounded ≤ 200 retries unlike Swift's infinite loop), `flasher_state`, `should_clear_now` / `mark_cleared`.
 - `starry-rs/starry-core/src/skyline_renderer.rs` - Per-frame sprite emitter. Accumulates fractional sprite quotas per-second (Swift parity), drains them into instanced `SpriteInstance`s for stars, building lights, and the flasher. `POINT_SPRITE_SIZE = 1.5`.
 - `starry-rs/starry-core/src/shooting_stars.rs` - `ShootingStarsRenderer` ports `ShootingStarsLayerRenderer.swift`. Poisson spawn (per-frame Bernoulli `p = dt/avg`), 18-segment trail interpolation, 15% fade-in over the first 15% of streak length, head-sprite size = thickness. Emits additive `SpriteInstance`s for each active streak per frame; engine pairs with decay layer for the trail effect.
 - `starry-rs/starry-core/src/satellites.rs` - `SatellitesRenderer` ports `SatellitesLayerRenderer.swift`. Exponential next-spawn timer, flasher-constrained spawn band (`y_min = max(building_max_height, flasher_y + r + gap + d/2)`), at most one active satellite at a time. Emits one additive point-sprite per frame; engine pairs with decay layer for the trail effect.
 - `starry-rs/starry-core/src/moon.rs` - Moon phase math (Julian-day → synodic-month age → illuminated fraction + waxing/waning bool) ported from `Moon.swift`. Hardcoded constants `NEW_MOON_EPOCH_UNIX_SECS = 947_182_440.0` (2000-01-06 18:14:00 UTC reference new moon) + `SYNODIC_MONTH_DAYS = 29.530588853`. Traversal-arc position (left→right; `progress = (wall_seconds mod traversal_seconds) / traversal_seconds`; `x = lerp(r, W-r, progress)`, `y = baseY + archH·sin(π·progress)`). Override triangular wave (`p≤0.5 → 2p waxing; p>0.5 → 2-2p waning`). Pub `radius_from_percent(percent, viewport_width) → i32` helper. `MoonParams` GPU-shared struct + `MoonParams::from_frame_state` factory live here too (matches `sprite.rs` hosting `SpriteInstance`). 5 unit tests.
 - `starry-rs/starry-core/src/moon_texture.rs` - Procedural 64×64 base albedo ported from `MoonTexture.swift`: 7 hardcoded maria (Gaussian σ = radius·0.5) + per-pixel SplitMix64 noise + clamp to `[25, 240]` u8 range. CPU `upsample_nearest(src, src_size, dst_size) → Vec<u8>` for diameter-matched output (linear-filter sampler on GPU side smooths the pixel-art look). Pub `create_moon_texture(target_diameter, rng) → Vec<u8>` end-to-end helper. 5 unit tests including deterministic-output verification.
 - `starry-rs/starry-core/src/moon_renderer.rs` - Moon GPU pipeline: render-pipeline + bind-group-layout + 64B `MoonUniformsGpu` UBO (`repr(C)` Pod+Zeroable mirroring WGSL fields exactly) + linear-filter `Sampler` + `R8Unorm` `Texture` + `BindGroup` + `current_diameter: u32` tracker + stashed `moon_diameter_percent: f64`. `new(device, queue, format, viewport_width, &Config)` allocates everything; `resize(device, queue, viewport_width)` no-ops if the computed diameter hasn't changed (otherwise regenerates the upsampled texture + rebinds); `draw(queue, pass, &MoonParams, viewport_w, viewport_h)` writes the UBO via `queue.write_buffer` (FIFO-safe inside a render pass, before submit), binds, draws 6 vertices. **`R8Unorm` has no row-alignment requirement**, so `bytes_per_row: Some(diameter)` works even when `diameter < 256`.
 - `starry-rs/starry-core/src/planet.rs` - Per-planet simulation state ported from `Planet.swift`. `PlanetIdentity` enum (8 variants, no Earth: Mercury / Venus / Mars / Jupiter / Saturn / Uranus / Neptune / Pluto) drives the per-planet texture cache + per-planet UBO writes. Keplerian J2000 ephemeris (semi-major axis, eccentricity, inclination, longitude of ascending node, argument of perihelion, mean anomaly at epoch + per-element drift rates) projects to heliocentric ecliptic XYZ → geocentric → equatorial → topocentric alt/az with a hardcoded **Austin TX observer** (`OBSERVER_LAT_DEG = 30.2672, OBSERVER_LON_DEG = -97.7431`). 3 below-horizon modes: `Random` (always randomized), `Hide` (skip when below), `RandomWhenBelow` (orbital when visible, randomized otherwise). Random fallback positions seed from a per-engine `nonce: u64` rooted in `Config::seed` so `(seed, w, h)` is reproducible across runs. Computes `PlanetParams` (13 fields packed into 3 `vec4`s for the UBO push). Saturn ring tilt + position-angle math LANDED as `saturn_ring_state(now)` (Schlyter B-formula `B = arcsin(sin β · cos 28.06° − cos β · sin 28.06° · sin(λ − Nr))` for tilt + RA/Dec-relative-to-celestial-pole rotation), with `Option<f64>` overrides feeding manual angle modes. **Phase 5c**: per-moon Keplerian orbital ephemeris for the 4 Galilean moons (Io, Europa, Ganymede, Callisto) and Saturn's Titan via `MoonSpriteState` struct + `moon_sprite_states(parent, wall_now, parent_radius_px, parent_center_px, ring_tilt_deg, ring_rotation_deg, scale)` standalone function — semi-major-axis-in-parent-radii + sidereal-period + initial-phase per moon, ecliptic XY → foreshorten Y by `sin(ring_tilt_deg)` (3° hardcoded for Jupiter; Saturn uses live ring-tilt) → 2D-rotate by ring-rotation → scale by parent radius → near/far-side z-cull (skip when `y > 0 && dist < parent_radius`); sprite size = `clamp(base_size · scale · clamp(parent_r / base_ref, 0.6, 1.8), 1.0, 3.0)` with `base_ref = 24` (Jupiter) / `20.3` (Saturn).
 - `starry-rs/starry-core/src/planet_texture.rs` - Procedural RGBA8 generators ported from `PlanetTexture.swift`: 8 unique pixel-art textures (Mercury weathered surface; Venus solid atmosphere; Mars red surface with surface noise; Jupiter horizontal bands + Great Red Spot + limb darkening + per-pixel noise; Saturn pale-gold banded body-only texture with double limb darkening — no rings baked in since they're rendered geometrically; Uranus + Neptune ice-giant gradients; Pluto small + sparse). Each generator builds a square RGBA8 buffer at a target resolution, with deterministic SplitMix64-style hashing for noise. **Mipmap chain generated at upload time** (`generate_mipmaps` helper in `planet_renderer.rs`) for Swift parity — small-planet-at-distance scenarios would look bad without mips. Sampled with `nearest` filter (intentionally different from the moon's `linear` filter) to preserve the retro pixel-art look on Jupiter's bands and Mars's surface.
 - `starry-rs/starry-core/src/planet_renderer.rs` - Planet GPU pipeline: render-pipeline + bind-group-layout + 48B `PlanetParamsGpu` UBO (3 × `vec4` packing 13 fields, `repr(C)` Pod+Zeroable mirroring WGSL exactly) + nearest-filter `Sampler` + `HashMap<PlanetIdentity, wgpu::Texture>` keyed by planet identity (O(1) per-planet lookup, selective regeneration on resize). `new(device, queue, format, viewport_w, viewport_h, &Config, rng)` allocates everything + populates the texture cache with mipmap chains for all visible planets; `resize(device, queue, viewport_w, viewport_h, &Config, rng)` regenerates only the planets whose computed diameter changed; `draw_all(queue, pass, &[PlanetParams], viewport_w, viewport_h)` iterates the visible planet list and binds + writes the UBO + draws 6 vertices per planet. `Option<PlanetRenderer>` in `gpu.rs` so `--planets-enabled false` fully skips GPU resource allocation (pipeline, BGL, texture cache, sampler) — not just the per-frame draw call.
 - `starry-rs/starry-core/src/engine.rs` - Simulation orchestrator: owns `Skyline` + `SkylineRenderer` + `Option<SatellitesRenderer>` + `Option<ShootingStarsRenderer>` + `Option<Moon>` + `Vec<Planet>` (empty when `--planets-enabled false`; otherwise holds all 8 `PlanetIdentity::ALL` entries) + per-engine `nonce: u64` rooted in `Config::seed` for deterministic random fallback positions + reused `planet_params_buf` + `planet_moons_sprite_buf` scratch + seeded `StdRng` + dt clock. **Wall-clock time is an injected parameter**: `frame(wall_now: SystemTime)` ticks against wall-clock with `MAX_DT_SECONDS = 0.25` clamp; `frame_with_dt(dt, wall_now)` trusts the caller (no clamp — used by headless at dt=5.0s for visually-rich single-frame output); both delegate to `frame_impl(dt, wall_now)`, which calls `Moon::position`/`Moon::illuminated_fraction` + each `Planet::frame_state` with `wall_now` + emits planet-moon sprites in a single inline pass via `moon_sprite_states(...)` per visible Jupiter/Saturn (with `--debug-moon-colors` color override happening in the same loop), and builds `MoonParams::from_frame_state(...)` + per-planet `PlanetParams`. Returns `FrameOutput { skyline_sprites, clear_skyline, satellites: Option<LayerFrame>, shooting: Option<LayerFrame>, moon: Option<MoonParams>, planets: &[(PlanetIdentity, PlanetParams)], planet_moons: &[SpriteInstance], debug_overlay: Option<DebugOverlayFrame> }` where `LayerFrame { sprites, decay }` bundles the per-layer sprite stream with its decay `keep_factor`. **Phase 6b** added the 9th `FrameOutput` field (`debug_overlay`, carries owned `String`s — ~30 bytes/frame deemed negligible — populated only when `--debug-overlay-enabled true`) and a `DebugSmoothers` struct on the Engine: FPS via 500ms accumulation window + `0.6/0.4` EMA, CPU% via per-frame `(delta_cpu_seconds / dt) × 100` + `0.8/0.2` EMA, both with `Option::None` first-sample guards. Stats text format `"FPS={:.1} CPU={:.1}%"` matches Swift's `"FPS=%.1f CPU=%.1f%%"` printf format.
 - `starry-rs/starry-core/src/gpu.rs` - `GpuPipelines` — **window-agnostic** wgpu pipeline owner. Holds `Device` + `Queue` + `TextureFormat` + skyline `Texture` + `Option<DecayLayer>` for flasher, satellites, and shooting (so `--*-enabled false` and `--flasher-period-s 0` fully skip GPU allocation) + skyline `SpriteRenderer` + `CompositeRenderer` + `Option<MoonRenderer>` (Option-skipped when `--moon-enabled false`) + `Option<PlanetRenderer>` (Option-skipped when `--planets-enabled false`) + `Option<SpriteRenderer>` for planet-moons (capacity 5, `BlendMode::Over`, Option-skipped when `--planet-moons-enabled false`) + `Option<DebugOverlayRenderer>` (Option-skipped when `--debug-overlay-enabled false`, owns its own pipeline + R8 glyph atlas + grow-on-demand instance VBO, single pipeline serves both text glyphs and BG rects via DEL=127 sentinel). Constructor `new(device, queue, format, width, height, &Config)`; renders into a caller-supplied `&TextureView` via `render_to_view(target_view, FrameOutput)`. Exposes `device()`/`queue()`/`format()` accessors so `WindowedGpu` doesn't duplicate them. `DecayLayer { tex_a, view_a, tex_b, view_b, active_is_a, sprites, decay, blend_mode }` owns each layer's full ping-pong state plus its sprite blend mode (`Additive` for satellites + shooting; `Over` for flasher — snap-on at ON, exponential fade during OFF). Per-frame 8-pass encode: (1) satellites decay → swap; (2) shooting decay → swap; (3) flasher decay → swap; (4) skyline sprite pass with `LoadOp::Clear(LAYER_WIPE_COLOR)` if `clear_skyline` else `Load`; (5) satellites sprite pass (Additive); (6) shooting sprite pass (Additive); (7) flasher sprite pass (Over) — emits at most one sprite per frame during ON-half; (8) composite pass into the target view `LoadOp::Clear(CLEAR_COLOR)` blending `[skyline, flasher?, satellites?, shooting?]` in Z-order, then drawing planets, then planet-moons, then the moon disc on top — all premultiplied alpha, all 3 sub-draws *inside the same composite pass* (encode pass count stays at 8). Resize rebuilds all textures and resets `active_is_a = true`; `MoonRenderer::resize` is delegated, and `PlanetRenderer` refreshes its texture cache lazily per-frame via `ensure_planet`, so resize just calls `set_viewport` on the planet-moons sprite renderer. `DecayLayer::new` is now 8-arg (added `blend_mode: BlendMode` + `label: &str`) and carries `#[allow(clippy::too_many_arguments)]` — internal-only with 3 call sites; deemed simpler than a config-struct refactor per the AGENTS.md "simplicity is highly valued" rule.
 - `starry-rs/starry-core/src/sprite.rs` - `SpriteInstance` (POD, `repr(C)`, position + scalar diameter + RGBA) + `BlendMode::{Over, Additive}` enum + `SpriteRenderer::new(device, format, capacity, mode)` (instanced quad pipeline parameterized by blend mode, viewport UBO, **grow-on-demand instance VBO** that resizes to `n.next_power_of_two().max(capacity * 2)` on overflow). Skyline uses Over; satellites + shooting use Additive.
 - `starry-rs/starry-core/src/decay.rs` - `DecayRenderer` fullscreen-quad pass that runs `out = textureLoad(src) * keep_factor` from one texture into another. Per-layer instance owns its own UBO (FIFO `write_buffer` ordering means one renderer per layer avoids overwriting in-flight uniforms). Called by `gpu.rs::run_decay_layer()` which then triggers `DecayLayer::swap()`.
 - `starry-rs/starry-core/src/composite.rs` - `CompositeRenderer`: stateless N-layer compositor. `draw_all(device, pass, &[&TextureView])` builds a temporary bind group per layer view and blends each over the swapchain in slice order with `PREMULTIPLIED_ALPHA_BLENDING`. No caching, no `rebind()` — just rebuild every frame; it's essentially free on the GPU and saves an entire class of bookkeeping bugs.
 - `starry-rs/starry-core/src/font.rs` - **Phase 6b** (~150L). Hand-rolled 128-entry 5×7 procedural bitmap glyph table — no font dependency, retro pixel-art aesthetic matching the moon + planets + skyline. Each glyph is a `[[bool; 5]; 7]` (35 bits, top-down rows). Public surface: `GLYPH_W = 5`, `GLYPH_H = 7`, `glyph_bitmap(c: char) -> Option<&'static [[bool; 5]; 7]>`. Sentinel: `t[127] = DEL` rendered as a solid 5×7 block — used by `debug_overlay.rs` for the background-rectangle pills (one pipeline + one atlas serves both text glyphs AND BG rects). 4 unit tests cover ASCII coverage + DEL sentinel + alphabetic/digit/punctuation completeness.
 - `starry-rs/starry-core/src/cpu_sample.rs` - **Phase 6b** (~125L). Per-OS process-CPU sampler with 3 `#[cfg(...)]`-gated impls: macOS uses Mach `task_info(MACH_TASK_BASIC_INFO + TASK_THREAD_TIMES_INFO)` to sum user+system+thread time; Linux parses `/proc/self/stat` fields 14+15 (`utime`+`stime`) divided by `sysconf(_SC_CLK_TCK)`; Windows uses `GetProcessTimes` FILETIME via `windows-sys`. Public surface: `CpuSampler::new()` + `sampler.sample_total_seconds() -> Option<f64>` returning cumulative CPU-seconds (delta-mode handled by the caller in `engine.rs::DebugSmoothers`). 1 unit test verifies monotonicity across two consecutive samples.
 - `starry-rs/starry-core/src/debug_overlay.rs` - **Phase 6b** (~21.5KB). `DebugOverlayRenderer` — single GPU pipeline + single R8 glyph atlas (packed 5×7 + DEL block) + single grow-on-demand instance buffer (initial capacity 128, grow `n.next_power_of_two().max(cap*2)` on overflow). 48-byte instance schema: `position [f32;2] @ 0..8` + `size [f32;2] @ 8..16` + `uv_min [f32;2] @ 16..24` + `_pad [f32;2] @ 24..32` + `tint [f32;4] @ 32..48`. Renders BOTH overlays from one `DebugOverlayFrame`: top-left stats pill (fuchsia text on dark-purple α=0.55 BG, 8px margin, `"FPS={:.1} CPU={:.1}%"` re-rasterized every 250ms) and bottom-right build-info pill (green text on dark-green α=0.55 BG, 8px margin, displays `BUILD_COMMIT`). Hardcoded constants `GLYPH_SCALE = 2`, `GLYPH_ADVANCE_PX = 10`, `LINE_HEIGHT_PX = 16`, `MARGIN_PX = 8`, `BG_PADDING_PX = 4`. CPU-side layout uses top-down screen-px Y; shader flips to NDC via `1.0 - (y/H) * 2.0`. Spaces are skipped (no atlas slot, no instance). Public API: `DebugOverlayFrame { stats_text, build_info_text }`, `DebugInstance` (48B `repr(C)` Pod+Zeroable), `DebugOverlayRenderer::new(device, queue, format)`, `set_viewport(w: u32, h: u32)`, `set_instances(queue, &[DebugInstance])`, `draw(pass)`, plus `layout_instances(frame, viewport_w, viewport_h, atlas_uv_table) -> Vec<DebugInstance>` exposed for unit testing. 9 unit tests cover viewport, padding, BG-rect generation, multi-line wrap, instance count math, empty-frame skip, atlas UV lookup.
 - `starry-rs/starry-core/src/headless.rs` - `dump_png(&Config)` — offscreen `Rgba8UnormSrgb` render-to-texture, drives a real `Engine` at fixed `dt = 5.0s` for deterministic byte-stable output. Wall-clock anchored at `HEADLESS_NOW_UNIX_SECS` (`1_704_067_200` = 2024-01-01 UTC) so the moon's phase + position are byte-stable too. **Phase 6c** promoted the const out of `headless.rs` into `crate::config` (`pub const HEADLESS_NOW_UNIX_SECS: u64`) so windowed deterministic-mode default and headless share the same literal — `headless.rs` now imports it. **Phase 6c** also clarified that headless `--dump-png` ignores `--time-mode` entirely; layering det-mode on top would just confuse two orthogonal mechanisms. Single-frame so it skips the ping-pong machinery entirely (at `dt=5s` the decay layers mathematically collapse to ~0): up to 8 render passes into the same readback target in Swift Z-order — Pass 1 skyline-Over with `Clear(CLEAR_COLOR)`, Pass 2 flasher-Over with `Load` (if enabled), Pass 3 satellites-Additive with `Load` (if enabled), Pass 4 shooting-Additive with `Load` (if enabled), Pass 5 planets-PremulAlpha with `Load` (if any visible), Pass 6 planet-moons-Over with `Load` (if any planet-moon sprites emitted — gated lazily on `!frame_output.planet_moons.is_empty()` rather than the config flag, mirroring the planet-renderer pattern), Pass 7 moon-PremulAlpha with `Load` (if enabled), **Pass 8 debug-overlay-PremulAlpha with `Load` (if `frame_output.debug_overlay.is_some()` — same lazy gating pattern; draws above the moon so glyph text sits on top of the moon disc)**. Padded `copy_texture_to_buffer` readback, writes PNG via the `png` crate. Builds its own device/queue from scratch (not via `GpuPipelines`) so headless stays decoupled from the windowed pipeline API.
 - `starry-rs/starry-core/src/bench.rs` - **Phase 6d** (~340L). Surfaceless GPU benchmark harness driving the real `GpuPipelines::render_to_view` path against a single offscreen `Rgba8UnormSrgb` texture — same render orchestration as the windowed shell, no Surface acquisition. Public surface: `pub fn run_bench(cfg: &Config) -> Result<(), Box<dyn Error>>` (async via `pollster::block_on`). Mechanics: 1 warmup frame (discarded — absorbs lazy GPU init / shader compile / page-in skew that wrecks p99+max otherwise), then N timed frames each ending in `device.poll(wgpu::PollType::wait_indefinitely())?` so per-frame deltas reflect GPU completion (not just submission); wall-clock anchored at `UNIX_EPOCH + Duration::from_secs(HEADLESS_NOW_UNIX_SECS) + (i+1) * BENCH_DT_SECONDS` (warmup uses base_now). Constants: `TEXTURE_FORMAT = Rgba8UnormSrgb`, `BENCH_DT_SECONDS = 1.0 / 60.0`. Stats computed via nearest-rank percentile (matches `cargo bench` + hyperfine; simpler than linear interp): mean / p50 / p95 / p99 / max in µs. CPU/frame derived from `CpuSampler::sample_process_cpu_seconds()` taken pre+post loop, difference / N. Output is dual: `println!` summary to stdout AND one appended row to `starry-rs/bench-results.md` (auto-headered on first write; in-tree path via compile-time `concat!(env!("CARGO_MANIFEST_DIR"), "/../bench-results.md")`; schema `| timestamp | tag | git_hash | frames | size | seed | mean µs | p50 µs | p95 µs | p99 µs | max µs | wall s | cpu s | cpu/frame µs |`). Default `--bench-tag` is `git rev-parse --short HEAD` + `+dirty` suffix from `git status --porcelain`, falling back to `"unknown"` if git isn't around. **Surfaceless caveat** (called out in the module docstring): no Surface means Fix A (winit `ControlFlow::Poll` → `Wait` + `pre_present_notify`) and Fix C (explicit `present_mode: Fifo`) gains cannot be observed here — those have to be measured via the dogfooded Phase 6b debug overlay running against the real winit shell.
 - `starry-rs/starry-core/src/config.rs` - clap-derive `Config` (63 pub fields: 12 Phase-2 + 15 Phase-3 across `shooting_stars_*` and `satellites_*` + 11 Phase-4 across `moon_*` and `debug_moon_colors` + 12 Phase-5a across `--planets-enabled`, 8 per-planet size flags [`--mercury-size`, `--venus-size`, `--mars-size`, `--jupiter-size`, `--saturn-size`, `--uranus-size`, `--neptune-size`, `--pluto-size`], `--planet-below-horizon-behavior`, `--planet-phase-mode`, `--planet-terminator-mode` + 3 Phase-5b across `--saturn-ring-style {smooth|flat-retro|chunky-pixel}`, `--saturn-ring-tilt-angle <-27..27>` (`Option<f64>`: absent = automatic Schlyter, present = manual override), `--saturn-ring-rotation-angle <0..360>` (`Option<f64>`: absent = automatic position-angle, present = manual override) + 3 Phase-5c across `--planet-moons-enabled`, `--jupiter-moon-scale <f64>`, `--saturn-moon-scale <f64>` (the existing `--debug-moon-colors` flag from Phase 4 does double duty in Phase 5c, toggling both lunar-moon raw-albedo and per-planet-moon hue-distinction modes); bools use `clap::ArgAction::Set` so `--flag false` disables) + 1 Phase-6a (`--config <path>`, `Option<PathBuf>`: present = explicit TOML config path with hard-error-on-missing semantics; absent = auto-discover `$XDG_CONFIG_HOME/starry/config.toml` then `./starry.toml` with silent fallthrough) + 3 Phase-6c (`--time-mode {realtime|deterministic|frozen}` default realtime, `--fixed-dt <f64>` default `1.0/60.0`, `--time-anchor <u64>` default `HEADLESS_NOW_UNIX_SECS` = 1_704_067_200 = 2024-01-01 UTC) + 1 Phase-6b (`--debug-overlay-enabled <bool>`, default `false` — single flag collapses Swift's 2-flag `debugOverlayEnabled` + `showBuildInfo` since both overlays share one renderer/atlas/pipeline) + `RingStyle` enum (3 variants, `From<RingStyle> for i32` mapping `Smooth=0, FlatRetro=1, ChunkyPixel=2`; grew `serde::{Serialize, Deserialize}` derive in Phase 6a with `#[serde(rename_all = "kebab-case")]`) + `TimeMode` enum (Phase 6c, 3 variants `Realtime / Deterministic / Frozen`, same serde derives as `RingStyle`). Four enums in this module/`planet.rs` (`BelowHorizonBehavior`, `PlanetPhaseMode`, `RingStyle`, `TimeMode`) all carry serde derives for TOML deserialization. Constants: `CLEAR_COLOR` (opaque black sky, per Swift parity) + `LAYER_WIPE_COLOR` (transparent black for the persistent skyline layer's periodic wipe) + `SPRITE_CAPACITY = 131_072` + rate helpers that scale Swift's reference per-second rates by canvas area + `PLANET_TERMINATOR_WIDTH = 0.1` and `PLANET_TERMINATOR_BANDS = 3` hardcoded constants for the planet shader. Phase 6b added `pub const BUILD_COMMIT: &str = git_version!(fallback = "unknown")` — compile-time short git hash via the `git-version` proc-macro, baked into the debug overlay's bottom-right build-info pill (replaces Swift's `BuildInfo.swift` Run-Script approach with a single proc-macro line).
 - `starry-rs/starry-core/src/toml_config.rs` - **Phase 6a** (NEW, ~700L). `PartialConfig` struct mirrors `Config`'s 58 non-`--config` fields wrapped in `Option<T>`, derives `serde::Deserialize` with `#[serde(deny_unknown_fields, rename_all = "kebab-case")]`. `apply_over(&self, dst: &mut Config)` walks each field and replaces `dst` only when `self.<field>.is_some()`. `cli_partial_from_matches(&ArgMatches) -> PartialConfig` extracts the user-explicit subset using `ArgMatches::value_source(name) == ValueSource::CommandLine` to distinguish "user typed `--seed 42`" from "clap filled in default 42". `load_config_from_env() -> Result<Config>` orchestrates the full precedence chain: start from `Config::default()` (which itself uses `Self::try_parse_from(["starry-rs"])` so clap stays the single source of truth for default values) → apply TOML partial via `apply_over` → apply CLI partial via `apply_over`. Path discovery: explicit `--config <path>` missing = hard error (`std::io::Error::NotFound` propagated; an explicit flag is an explicit promise); auto-discovery walks `$XDG_CONFIG_HOME/starry/config.toml` then `./starry.toml`, silently falling through if absent. 7 unit tests including `partial_field_count_matches_config` which hard-codes `EXPECTED = 58` to catch field-count drift if someone adds a Config flag without mirroring it in PartialConfig.
 - `starry-rs/starry-core/src/types.rs` - `Color { r, g, b: f32 }` + `Point { x, y: i32, color: Color }` value types + `random_star_color` producing pastel blue/purple/red mixes (matches Swift's `r:[0,0.5] g:[0,0.5] b:[0,1.0]` ranges) + `debug_moon_color_premul(name)` helper for `--debug-moon-colors` mode (zero-alloc lowercase-name match; returns premultiplied-alpha RGBA per Galilean/Titan moon: Io=red, Europa=green, Ganymede=blue, Callisto=cyan, Titan=yellow at α = 1.0 — Swift parity with `StarryEngine.swift::debugMoonColorPremul`).
 - `starry-rs/starry-core/src/shader.wgsl` - Sprite vertex (pixel→NDC via viewport UBO, Y-up matching Swift) + fragment (round-disc with soft edge, premultiplied output) shaders.
 - `starry-rs/starry-core/src/decay.wgsl` - Fullscreen-triangle vertex + fragment that samples `src` via `textureLoad`, multiplies by `uniforms.keep`, and **subtracts a tiny eps (`1.0 / 2048.0` ≈ 0.000488 linear) clamped at 0** to escape the sRGB-8 quantization fixed point. Without the eps, sRGB-8 textures hit a stuck residue at low brightness because `round(N × keep) = N` once `N ≤ floor(0.5 / (1 − keep))` — shoot keep=0.938 froze visibly at sRGB-8; eps shifts the rounding outcome at the low end without perceptibly changing brighter values. One per-layer UBO; called once per decay layer per frame.
 - `starry-rs/starry-core/src/moon.wgsl` - Moon vertex (6-vertex NDC quad sized + positioned from `MoonUniformsGpu`) + fragment (round-disc with `r²>1` discard, soft edge `featherLocal = clamp(2/radius, 0.0015, 0.12)`, terminator math `cos_delta = 1 - 2·illum; delta = acos(...); phi = waxing>0 ? PI-delta : delta-PI`, 3 modes [hard step / smooth gradient / banded], bright/dark hemisphere lerp using sampled R8 albedo, debug-colors override). UBO field order = explicit Rust `MoonUniformsGpu` mirror: `viewport_size`, `center_px`, `params0 = (radius, illum, bright, dark)`, `params1 = (debug_mask_flag, waxing_sign, _, _)`, `params2 = (terminator_mode, terminator_width, terminator_bands, _)`. 64B total.
 - `starry-rs/starry-core/src/planet.wgsl` - Shared planet vertex + fragment shader, parameterized by per-planet UBO (`PlanetParamsGpu`, 13 fields packed into 3 `vec4`s). Vertex: 6-vertex NDC quad sized from `radius_px × texture_aspect` (1.0 for non-Saturn, 2.0 for Saturn so rings have horizontal real-estate). Fragment: branches on `is_saturn = aspect > 1.0` (matches `Shaders.metal:312`). Non-Saturn path: round-disc with soft edge + albedo sample (`textureSample` with nearest filter) + `limb_darkening_factor(nx, ny)` shading + 3 terminator modes (hard / smooth / banded) using same `phi` math as moon shader. Saturn path: body inside `r ≤ SATURN_BODY_RADIUS = 0.846` (double limb darkening: inner `1.0 - 0.20·r²` + outer `limb_darkening_factor`); rings across `r ∈ [0.9306, 1.9458]` with 7-zone radial band selector `(C / B-inner / B-outer / Cassini gap / A-inner / Encke gap / A-outer)`, edge-on guard `|sin_tilt| ≥ 0.01`, 3 user-pickable styles (`smooth` continuous / `flat-retro` 4-color luminance quant + 2×2 Bayer alpha dither / `chunky-pixel` 16-step radial quant + checker dither). 4 hardcoded constants (`SATURN_BODY_RADIUS`, `SATURN_RING_INNER`, `SATURN_RING_OUTER`, `RING_BRIGHTNESS = 0.92`).
 - `starry-rs/starry-core/src/composite.wgsl` - Composite vertex (3-vert fullscreen triangle via `vertex_index`) + fragment (`textureLoad` passthrough — blending happens in the pipeline's blend state, not the shader).
 - `starry-rs/starry-core/src/debug.wgsl` - **Phase 6b** (~70L). Textured-quad shader for the debug overlay. Vertex: `TriangleStrip` 4-vertex; corner derived from `vertex_index` via `(v & 1u, (v >> 1u) & 1u)`; converts top-down screen-px instance position + size to NDC by `1.0 - (y / viewport_h) * 2.0` (Y flip). Fragment samples the R8 glyph atlas with `textureSample` (linear filter) and multiplies by per-instance `tint: vec4<f32>`. Single pipeline serves both text glyphs (atlas UV → real glyph cell) and BG rect pills (atlas UV → DEL sentinel solid cell), so one draw call handles both surfaces.

**`starry-app/` — binary crate (winit shell; owns the Surface + main event loop):**
 - `starry-rs/starry-app/Cargo.toml` - Binary manifest. `[[bin]] name = "starry-app"`. Deps via `.workspace = true`: `starry-core` (path), wgpu, winit, pollster, log, env_logger, clap.
 - `starry-rs/starry-app/src/main.rs` - Entry point: env_logger init, `Config::parse()` via clap, dispatch to either the windowed path (`app::run`) or `starry_core::headless::dump_png(&config)`. Two `mod` declarations: `mod app; mod gpu;`.
 - `starry-rs/starry-app/src/app.rs` - winit `ApplicationHandler` impl: window creation, GPU lifecycle, redraw loop. Owns `Engine` (from `starry_core::engine`) and `WindowedGpu`; rebuilds `Engine` (preserving seed) on resize since the skyline geometry is resolution-dependent.
 - `starry-rs/starry-app/src/gpu.rs` - `WindowedGpu` — surface + swap-chain wrapper around `GpuPipelines`. Owns `Surface`, `SurfaceConfiguration`, and a `GpuPipelines` instance. Creates the wgpu Instance, picks adapter (high-perf, surface-compatible), creates Device+Queue, picks surface format (sRGB-preferred), then constructs `GpuPipelines`. `resize` reconfigures the surface then delegates. `render` acquires `SurfaceTexture`, handles `Lost`/`Outdated`/`Timeout`/`Other` error variants (reconfigure-and-retry, swallow-and-skip respectively), creates the view, calls `pipelines.render_to_view`, presents.

**Top-level:**
 - `starry-rs/README.md` - Build/run instructions, phased roadmap, CLI reference tables (Phase 2 + Phase 3 + Phase 4 + Phase 5a + Phase 5b), headless usage, layout notes.
 - `starry-rs/PORT_PLAN.md` - In-tree engineering scratchpad: granular phase scoping, active todos, decisions log, validation history, open questions. Committed alongside the work it tracks; the user-facing roadmap lives in `README.md`.

### Conventions specific to the Rust side
 - **Pinned wgpu version.** Stay on `wgpu = "29"` until a deliberate, scoped upgrade. wgpu reshapes its API between minor releases — 29 alone introduced `experimental_features`, `multiview_mask`, `depth_slice`, and replaced `SurfaceError` with the `CurrentSurfaceTexture` enum. Bumping mid-port is a recipe for sadness. All version pins live in `starry-rs/Cargo.toml` under `[workspace.dependencies]`; member manifests pull in via `.workspace = true` so the wgpu pin (and every other dep version) is single-source.
 - **Workspace layout — `starry-core` (lib) + `starry-app` (bin).** `starry-core` owns all simulation + GPU pipeline + WGSL + headless code (window-agnostic; suitable for headless embedders and future tooling). `starry-app` is the thin winit shell: `Surface`, `SurfaceConfiguration`, instance/adapter/device/queue setup, main event loop, and CLI dispatch. The key split is in `gpu.rs`: `GpuPipelines` (in `starry-core`) owns Device+Queue+all pipelines and renders into a caller-supplied `&TextureView`; `WindowedGpu` (in `starry-app`) wraps `GpuPipelines` with a `Surface`. `default-members = ["starry-app"]` in the workspace `Cargo.toml` so `cargo run` from workspace root still targets the binary without needing `-p starry-app`.
 - **No re-exports in `starry-core/lib.rs`.** 24 `pub mod` declarations, no `pub use`. Call sites use explicit paths (`use starry_core::config::Config`) — keeps the import surface honest and avoids re-export drift.
 - **TOML config = clap mirror, not a parallel schema.** Phase 6a's `PartialConfig` mirrors `Config`'s flags 1:1 in kebab-case (`stars-fraction`, not `[stars] fraction`). Adding a new flag is a 4-edit job: Config field → PartialConfig field → `cli_partial_from_matches` extractor → `apply_over` setter. The `partial_field_count_matches_config` test hard-codes `EXPECTED = 58` (Config has 59 pub fields = 58 mirrored + 1 `--config` deliberately omitted to avoid chicken/egg). When you bump it, bump `EXPECTED` too — the comment in the test explains the off-by-one so future flag-adders aren't confused. Any enum that needs to be TOML-deserializable derives `serde::{Serialize, Deserialize}` with `#[serde(rename_all = "kebab-case")]` so TOML stays visually consistent with CLI (`"random-when-below"`, not `"RandomWhenBelow"`).
 - **WGSL shaders live next to the `.rs` that includes them** (`shader.wgsl` next to `sprite.rs`, etc.). `include_str!()` paths are file-relative, so moving a `.rs`/`.wgsl` pair together requires zero shader-path edits.
 - **Wall-clock time is injected, not fetched.** Any simulation code that depends on real-world time (Phase 4 onwards: the moon phase, Phase 5a's planet ephemerides) takes a `wall_now: SystemTime` parameter rather than calling `SystemTime::now()` internally. The windowed shell (`app.rs`) passes `SystemTime::now()` from the redraw handler; headless (`headless.rs`) passes a hardcoded `UNIX_EPOCH + Duration::from_secs(HEADLESS_NOW_UNIX_SECS)` (2024-01-01 UTC) for byte-stable determinism. Same pattern as `dt` injection in Phase 3 — and same payoff: deterministic headless tests, no environment dependency.
 - **Visual ground-truth = Swift.** When porting a layer, read the Swift source carefully and match behavior pixel-ish-for-pixel-ish. [`METAL_RENDERER_MAP.md`](METAL_RENDERER_MAP.md) is the architectural cheat sheet (5 pipelines: spriteOver, spriteAdditive, decayInPlace, moon, composite; ping-pong layer textures).
 - **Determinism is a feature.** Both the windowed shell and headless mode seed all randomness from `Config::seed`. Resize rebuilds the `Engine` with the same seed so a given `(seed, w, h)` is reproducible.
 - **Headless SHA parity = gold-standard refactor test.** When refactoring shared code (Phase 3.5 was the first big trial run), `cargo run -- --dump-png /tmp/x.png` against the seed=42 1280×800 default and `shasum -a 256` it. A matching SHA against the pre-refactor baseline proves byte-for-byte parity of the entire simulation + skyline GPU path. Doesn't cover the windowed Surface acquisition path — that one always needs a human eyeball. **Current baselines**: Phase 2 = `c4fac4d7...`; Phase 3 + 3.5 (sky+sat+shoot, no moon) = `476e74bc...`; Phase 4 (sky+sat+shoot+moon, smooth-mode terminator default, wall_now = 2024-01-01 UTC) = `986155e2...`; Phase 5a default (sky+sat+shoot+moon+7 planets) = `bc6fd368...` (superseded by 5b); Phase 5b default (sky+sat+shoot+moon+8 planets, Saturn body + rings) = `fc7d5fdc...` (superseded as default by 5c, but still serves as the planets-enabled-but-planet-moons-off SHA in 5c); **Phase 5c default (sky+sat+shoot+moon+8 planets+5 planet-moons) = `1e0f0067...`**; Phase 5c with `--debug-moon-colors true` = `b2f42ad3...`; planets-off (any phase ≥ 5a) = `986155e2...` (Phase-4 byte-identical, proving `Option<PlanetRenderer>` cleanly skips planet GPU resources through the new Saturn shader path too); `--planet-moons-enabled false` (Phase 5c) = `fc7d5fdc...` (Phase-5b byte-identical, proving `Option<SpriteRenderer>` cleanly skips planet-moon GPU resources without altering anything else).
 - **Rust style.** Standard `cargo fmt` + `cargo clippy --workspace --all-targets -- -D warnings` cleanliness. Same simplicity/readability bar as the Swift code — fancy optimizations only when they meaningfully help.


## Documentation Guidelines
Any changes to code must be paired with updates to the relevant documentation if they materially change behavior, file roles, or architecture. This includes (but is not limited to):
 - Adding, removing, or renaming source files → update the File Contents section above.
 - Changing the rendering pipeline → update [METAL_RENDERER_MAP.md](METAL_RENDERER_MAP.md).
 - Adding or completing a feature → update the Status checklist in [README.md](README.md).
 - Touching the Rust port (`starry-rs/`) → update [`starry-rs/README.md`](starry-rs/README.md) (phase status, file layout, dep versions) and the Rust section in this file when conventions change.


## Coding Guidelines
Simplicity and understandability are highly valued. Fancy performance optimizations should only be used if they will significantly improve rendering speed or efficiency. 

Swift best practices must be followed unless we have a tremendously compelling reason to deviate from them.


## Building, Installing & Running

The Xcode project has two targets:

| Target | Scheme | Product | What it is |
|--------|--------|---------|------------|
| `StarryExcuseForAMacScreensaver` | `StarryExcuseForAMacScreensaver` | `StarryExcuseForAMacScreensaver.saver` | The screensaver bundle |
| `StarryPreview` | `StarryPreview` | `StarryPreview.app` | Standalone preview app (borderless window, no System Prefs needed) |

### Build from the command line

```bash
# Build the screensaver bundle (Debug)
xcodebuild -project StarryExcuseForAMacScreensaver.xcodeproj \
  -scheme StarryExcuseForAMacScreensaver \
  -configuration Debug \
  build

# Build the preview app (Debug)
xcodebuild -project StarryExcuseForAMacScreensaver.xcodeproj \
  -scheme StarryPreview \
  -configuration Debug \
  build
```

Both products land in DerivedData:
```
~/Library/Developer/Xcode/DerivedData/StarryExcuseForAMacScreensaver-<hash>/Build/Products/Debug/
```

### Install the screensaver

Copy the built `.saver` bundle into the user Screen Savers directory:
```bash
BUILT="$(xcodebuild -project StarryExcuseForAMacScreensaver.xcodeproj \
  -scheme StarryExcuseForAMacScreensaver -showBuildSettings 2>/dev/null \
  | grep ' TARGET_BUILD_DIR' | xargs | cut -d' ' -f3)"

cp -R "$BUILT/StarryExcuseForAMacScreensaver.saver" ~/Library/Screen\ Savers/
```

After copying, open **System Settings → Screen Saver** and select it. (On first install macOS may prompt you to approve the bundle.)

### Run the preview app

The fastest way to visually verify changes — no screensaver activation required:
```bash
BUILT="$(xcodebuild -project StarryExcuseForAMacScreensaver.xcodeproj \
  -scheme StarryPreview -showBuildSettings 2>/dev/null \
  | grep ' TARGET_BUILD_DIR' | xargs | cut -d' ' -f3)"

open "$BUILT/StarryPreview.app"
```

Or equivalently, run the `StarryPreview` scheme directly from Xcode (⌘R with that scheme selected).

### Build & run the Rust port

The Rust crate is self-contained under `starry-rs/`. You need a Rust toolchain via [rustup](https://rustup.rs) (`sh.rustup.rs`); make sure `$HOME/.cargo/env` is sourced in your shell.

```bash
# Build the whole workspace (debug)
cargo build --manifest-path starry-rs/Cargo.toml --workspace

# Build + launch the window (workspace default-member is `starry-app`, so no -p needed)
cargo run --manifest-path starry-rs/Cargo.toml

# With logging
RUST_LOG=info cargo run --manifest-path starry-rs/Cargo.toml

# Clippy across the workspace
cargo clippy --manifest-path starry-rs/Cargo.toml --workspace --all-targets -- -D warnings

# Headless single-frame render (deterministic byte-stable PNG; great for refactor parity checks)
cargo run --manifest-path starry-rs/Cargo.toml -- --dump-png /tmp/starry.png
```

Or `cd starry-rs && cargo run`. See [`starry-rs/README.md`](starry-rs/README.md) for the full phased roadmap and CLI flag reference.


## Known Quirks

### Screensaver Defaults Live in a Sandboxed Container

On modern macOS (Ventura+), screensavers run inside the `com.apple.ScreenSaver.Engine.legacyScreenSaver` sandbox container. `ScreenSaverDefaults(forModuleWithName:)` persists preferences into that container's **ByHost** directory, not the user's global `~/Library/Preferences/`:

```
~/Library/Containers/com.apple.ScreenSaver.Engine.legacyScreenSaver/
  Data/Library/Preferences/ByHost/<bundle-id>.<hardware-uuid>.plist
```

This means a standalone app (like StarryPreview) can't read those settings through the normal `ScreenSaverDefaults` API — it resolves to a different, non-containerized location and finds nothing.

**Our workaround**: `PreviewAppDelegate.seedDefaultsFromScreenSaverContainer()` reads the plist directly from the container path and seeds the values into the local `ScreenSaverDefaults` instance before the engine starts. If the plist isn't found (screensaver was never configured), the built-in fallback defaults in `StarryDefaultsManager` take over gracefully.

**If this breaks after a macOS update**, check whether Apple changed:
 - The container name (`com.apple.ScreenSaver.Engine.legacyScreenSaver`)
 - The storage path within the container (`Data/Library/Preferences/ByHost/`)
 - The plist naming convention (`<bundle-id>.<hardware-uuid>.plist`)
