
## 4. Add the Icon to the Xcode Target

1. Drag `StarryPreview.icns` into Xcode’s Project Navigator (e.g. into a Resources group).
2. In the Add dialog, ensure the screen saver target checkbox is selected (Copy items if needed).
3. Clean & build.

## 5. Verify Info.plist Setting

`Info.plist` now contains:
